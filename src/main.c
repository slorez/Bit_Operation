#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "bitShift.h"


int main(int argc, char const *argv[])
{
/*	char cmdAll[4] = {"All"}; 
	int a = atoi(argv[1]);
	printf("Filename: {%s}\n", argv[0]);
	printf("Name: {%s}\n", argv[1]);
	printf("int a: %d\n", a);
	if (argv[1] == 'A')
		printf("Hello %s\n", argv[1]);*/
	printAll();
	return 0;
}

